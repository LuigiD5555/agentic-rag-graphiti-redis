"""Apps/plugins directory (Django-style).

This directory contains optional apps that extend the RAG system functionality.
Each app should have its own AppConfig in apps.py for registration via INSTALLED_APPS.
"""
