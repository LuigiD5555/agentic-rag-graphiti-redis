# Paths that should NOT be content-deduplicated during ingestion.
#
# Use this when file location is meaningful even if content is identical
# (e.g., Python package __init__.py files).
#
# Supported entries:
# - Basename: __init__.py
# - Glob: **/__init__.py
# - Directory (trailing slash): src/my_pkg/
# - Regex (prefix "re:"): re:^.*/__init__\\.py$

__init__.py
