# LangChain stack
langchain-core
langchain-community
langchain-text-splitters
langchain-experimental

# LangGraph for memory/state management
langgraph
# Note: SQLite control plane handles checkpoint persistence

# Storage / graph / cache
# External cache removed - using SQLite control plane instead
neo4j
weaviate-client>=4,<5
grpcio

# Ingestion (Office tools delegated to rag-tool-document-processor)
networkx
python-docx  # Still needed for DOCX native parsing (fallback to rag-tool-document-processor)
pypdf
pymupdf

# API
fastapi
uvicorn[standard]
python-multipart  # Required for file uploads in FastAPI
openai

# Utils
python-dotenv
requests
httpx
pytest
pytest-asyncio
pytest-readable @ git+https://github.com/LuigiD5555/pytest-readable.git@main
pydantic
pydantic-settings
langdetect

# SWARM RAG — ML models
torch
transformers
sentence-transformers
accelerate      # required by some HuggingFace seq2seq models
keybert         # keyword extraction (F2 perception upgrade)

# RabbitMQ for message queuing
pika==1.3.2
aio-pika==9.4.1
celery==5.3.6

# Code coverage for dynamic analysis
coverage>=7.0.0
